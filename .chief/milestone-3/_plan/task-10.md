# Task 10 — Rename `.devcontainers/<id>/` → `.devcontainer/<id>/` and rewire forwarders

## Goal

Make sandcontainer spec-compliant by adopting `.devcontainer/<id>/devcontainer.json` (devcontainer spec layout #3). Change the workspace passed to `@devcontainers/cli` from the template directory to the user's repository root, and select the per-template config via `--config <abs-path>`. Rewrite `down` to use the dual docker label filter so multiple templates in the same repo don't collide.

After this task, `scx init <id>` + `scx up <id>` + `scx exec <id> <cmd>` must succeed end-to-end against `@devcontainers/cli` without the `Dev container config ... not found` error.

## Scope

Files that change:

1. `src/commands/init.ts` — target path, stdout string, error string
2. `src/commands/list.ts` — scan root
3. `src/commands/up.ts` — pre-flight path, argv assembly
4. `src/commands/exec.ts` — pre-flight path, argv assembly
5. `src/commands/rebuild.ts` — pre-flight path, argv assembly
6. `src/commands/down.ts` — pre-flight path, docker filter args
7. `src/commands/*.test.ts` (6 files) — path literals and asserted argv
8. `README.md` — example output, Project Layout section, command cheatsheet
9. `.chief/_rules/_contract/filesystem.md` (new file)

Files that are **not** touched:
- `.chief/milestone-1/**` (historical per AGENTS.md)
- `templates/**` (no template format change)
- `catalog.json` (no catalog change)
- `.devcontainer/` (the repo's own dogfood — already on the new layout)
- `CHANGELOG.md` (auto-generated by release-it; see Commit Message section below)

## Runtime Contract (New)

For every forwarder, compute:

```ts
const repoRoot = path.resolve(cwd);                                        // absolute
const configPath = path.resolve(repoRoot, '.devcontainer', id, 'devcontainer.json');
```

Pre-flight existence check is always on `configPath`. If missing:

```
error: Template "<id>" is not initialized. Run: sandcontainer init <id>
```

(unchanged wording — the message references the CLI command, not the on-disk path, so no string edit is needed)

Invocations:

| Command | Argv |
|---|---|
| `up <id> ...args` | `npx @devcontainers/cli up --workspace-folder <repoRoot> --config <configPath> ...args` |
| `exec <id> ...args` | `npx @devcontainers/cli exec --workspace-folder <repoRoot> --config <configPath> ...args` |
| `rebuild <id> ...args` | `npx @devcontainers/cli up --workspace-folder <repoRoot> --config <configPath> --remove-existing-container ...args` |
| `down <id>` | `docker ps -q --filter label=devcontainer.local_folder=<repoRoot> --filter label=devcontainer.config_file=<configPath>` → pipe each matched id to `docker stop`. No matches → exit 0 silently. |

Flag ordering: put `--workspace-folder` and `--config` **before** `...forwardedArgs` for `up` / `exec`. For `rebuild`, put `--remove-existing-container` immediately after `--config` and before `...forwardedArgs` (matches the existing pattern where sandcontainer-injected flags precede user-forwarded flags).

## User-Facing String Changes

Exact before/after:

### `init.ts`

| Type | Before | After |
|---|---|---|
| stdout | `Initialized template "<id>" at .devcontainers/<id>/devcontainer.json\n` | `Initialized template "<id>" at .devcontainer/<id>/devcontainer.json\n` |
| stderr | `error: .devcontainers/<id>/devcontainer.json already exists. Use --force to overwrite.\n` | `error: .devcontainer/<id>/devcontainer.json already exists. Use --force to overwrite.\n` |

### `list.ts`

No user-visible string change. Still prints ids one per line or `No templates found.\n`. The scanned directory changes from `.devcontainers` to `.devcontainer`; files (not directories) under `.devcontainer/` are skipped — only directories containing `devcontainer.json` become template entries. This means a bare `.devcontainer/devcontainer.json` (spec layout #2) is ignored by `list` and treated as user-owned state.

### `up.ts` / `exec.ts` / `rebuild.ts` / `down.ts`

No user-visible string changes. Only argv / docker-filter changes per the Runtime Contract.

### `citty` command `description` fields

Update so `--help` output matches reality:

- `init`: `Download a devcontainer template into .devcontainers/<id>/` → `Download a devcontainer template into .devcontainer/<id>/`
- Other commands: no description change needed.

## Tests

Every `.test.ts` file under `src/commands/` references the old path or the old argv shape. Update them to match the new contract. Specifically:

- `init.test.ts`: replace `.devcontainers/` literals with `.devcontainer/` (4 occurrences in assertions + fs paths)
- `list.test.ts`: replace `.devcontainers` with `.devcontainer` (6 occurrences)
- `up.test.ts`: asserted argv changes — index 3 is now the repo root (cwd), and new indices assert `--config` + configPath. Update the "not initialized" test to use the new path for the `fs.existsSync` side of the check.
- `exec.test.ts` and `rebuild.test.ts`: same shape as `up.test.ts`
- `down.test.ts`: the docker `ps` argv now has two `--filter` flags. Assert both label strings exactly.

Do **not** add new test cases for untested behavior; keep the test suite scope identical to what exists today. If a test's existing assertion is now underspecified (e.g. only checks the first `--filter`), expand it just enough to pin the new behavior — no additional cases.

## README Changes

Replace every occurrence of `.devcontainers/` with `.devcontainer/` and every example argv to match the new forwarder shape:

- Line 3 (tagline): `.devcontainers/<id>/` → `.devcontainer/<id>/`
- Line 45 (`scx init` description): same
- Lines 49, 52, 55 (`scx init` example output): same
- Line 71 (`scx list` no-results comment): `in a project without .devcontainers/` → `in a project without .devcontainer/`
- Lines 80–85 (`scx up` examples): change `--workspace-folder .devcontainers/claude-code` to `--workspace-folder . --config .devcontainer/claude-code/devcontainer.json` (use `.` for brevity in the comment; code uses an absolute path)
- Lines 93–101 (`scx exec` examples): same pattern
- Line 109 (`scx rebuild` example): same pattern
- Line 118 (`scx down` example comment): update to reflect dual-label filter — `# → docker stop <container with labels devcontainer.local_folder=<repo> AND devcontainer.config_file=<config>>`
- Lines 155–160 (Project Layout block): rename `.devcontainers/` to `.devcontainer/`

No new README sections. No rewriting of the "Template Images" section or the command prose. Purely a find-and-replace of the path and argv shape.

## New Rule File

Create `.chief/_rules/_contract/filesystem.md`:

```markdown
# Filesystem Convention

sandcontainer-managed templates live at `.devcontainer/<id>/devcontainer.json` (devcontainer spec layout #3).

- **Directory**: `.devcontainer/` (singular, matching the devcontainer spec).
- **Per-template subdirectory**: `<id>/` where `<id>` is the catalog template id.
- **Config file**: `devcontainer.json` inside each subdirectory.

## Runtime Invocation

Forwarders invoke `@devcontainers/cli` with the user's repository root as the workspace folder and the per-template config as `--config`:

- `--workspace-folder = path.resolve(cwd)`
- `--config = path.resolve(cwd, '.devcontainer', id, 'devcontainer.json')`

## `down` Container Matching

`scx down <id>` identifies the target container by the dual docker label pair written by `@devcontainers/cli`:

- `devcontainer.local_folder = <repoRoot>`
- `devcontainer.config_file = <configPath>`

Both labels MUST match; either alone is insufficient because multiple templates under one repo share the `local_folder`.

## Pre-flight Check

Every forwarder (`up`, `exec`, `rebuild`, `down`) verifies that `<configPath>` exists before invoking `@devcontainers/cli` or `docker`. If absent, it prints `error: Template "<id>" is not initialized. Run: sandcontainer init <id>` to stderr and exits 1.

## Reserved Locations

sandcontainer does NOT manage:

- `.devcontainer/devcontainer.json` (root-level config, spec layout #2) — ignored by `list` and forwarders.
- `.devcontainer.json` (spec layout #1) — ignored.

These may exist alongside sandcontainer-managed template subdirectories without conflict.
```

## Commit Message

Single commit. Use conventional-commit format so release-it + @release-it/conventional-changelog generates the v0.2.0 CHANGELOG entry automatically.

```
fix!: use `.devcontainer/<id>/` layout and mount repo root as workspace

Aligns sandcontainer with the devcontainer spec (layout #3). Templates are
now stored at `.devcontainer/<id>/devcontainer.json` (singular), and
forwarders invoke `@devcontainers/cli` with `--workspace-folder <repoRoot>
--config <configPath>`. `down` matches containers via dual label filter
(`devcontainer.local_folder` + `devcontainer.config_file`).

BREAKING CHANGE: The on-disk layout changed from `.devcontainers/<id>/`
(plural) to `.devcontainer/<id>/` (singular). Users with templates
initialized under v0.1.x must re-run `sandcontainer init <id>` and delete
the orphan `.devcontainers/` directory manually. The container's mounted
workspace also changed from the template directory to the user's
repository root — `claude` and other tools now see the user's project
code at `/workspaces/<repo-name>` inside the container.
```

The `!` after `fix` and the `BREAKING CHANGE:` footer together trigger a minor version bump (0.1.x → 0.2.0) under 0.x semver rules as configured in `.release-it.json`.

## Verification (Before Commit)

Run locally:

```bash
pnpm lint       # tsc + eslint + prettier
pnpm test:ci    # vitest run
pnpm build      # tsup
```

All three MUST pass. Do not proceed if any fails.

Manual smoke test (optional but recommended — builder does NOT need to delegate to tester-agent unless the user requests it):

```bash
# from a scratch directory
node /path/to/sandcontainer/dist/main.js init claude-code
ls .devcontainer/claude-code/devcontainer.json      # should exist
node /path/to/sandcontainer/dist/main.js list       # should print: claude-code
# `up` and `exec` require docker; skip unless available
```

## Out of Scope

- Auto-migration from `.devcontainers/` to `.devcontainer/`
- Windows docker-label path normalization (drive-letter lowercasing)
- Version bump in `package.json` (release-it handles this)
- Any catalog.json or template format changes
- Retroactive edits to milestone-1 artifacts

## Success Criteria

- [ ] Every `.devcontainers/` literal in `src/` and `README.md` is replaced with `.devcontainer/` OR removed (no mixed usage).
- [ ] All four forwarders pass `--workspace-folder <repoRoot>` and `--config <configPath>` (or the equivalent docker-label pair for `down`).
- [ ] `pnpm lint`, `pnpm test:ci`, `pnpm build` all pass.
- [ ] `.chief/_rules/_contract/filesystem.md` exists with the text in the "New Rule File" section.
- [ ] Commit message uses `fix!:` prefix and includes the `BREAKING CHANGE:` footer.
